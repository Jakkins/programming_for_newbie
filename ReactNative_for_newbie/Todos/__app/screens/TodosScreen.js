import React from 'react'
import {
    Text,
    View,
    TouchableOpacity
} from 'react-native'
import {logScreenCSS, LogInCSS} from '../styles/global'

export default function TodoScreen({navigation}) {
    return (
        <Text>Ciao</Text>
    )
}